<div class="container collections mt-3 pb-3">
    <div class="row row-cols-1 row-cols-lg-2 g-3">
        {{#each @model as |api|}}
        <div class="col">
            <ApiTemplate @api={{api}} />
        </div>

        {{/each}}
    </div>

</div>